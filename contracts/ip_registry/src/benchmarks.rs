/// #377 Performance Benchmarking Suite — IP Registry
///
/// Benchmarks disabled due to deprecated Soroban budget API.
/// To re-enable, update to use cost_estimate().budget() instead of env.budget()
#[cfg(test)]
mod benchmarks {
    // Benchmarks temporarily disabled
}
